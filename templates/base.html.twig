<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Factures
			{% endblock %}
		</title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body
		class="bg-gray-100 text-gray-800">

		<!-- Barre de navigation -->
		<nav class="bg-indigo-600 text-white shadow">
			<div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
				<a href="{{ path('invoice_index') }}" class="text-lg font-bold tracking-wide">
					💼 Gestion des Factures
				</a>
				<div class="space-x-4">
					<a href="{{ path('invoice_index') }}" class="hover:underline">Liste</a>
					<a href="{{ path('invoice_new') }}" class="hover:underline">Nouvelle</a>
				</div>
			</div>
		</nav>

		<!-- Contenu principal -->
		<main class="max-w-6xl mx-auto px-4 py-6"> {% block body %}{% endblock %}
			</main>

			<!-- Footer -->
			<footer class="bg-gray-200 text-center py-4 text-sm text-gray-600">
				©
				{{ "now"|date("Y") }}
				- Gestion des factures avec Symfony & TailwindCSS
			</footer>
		</body>
	</html>

